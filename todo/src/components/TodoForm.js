import React from "react";

const TodoForm = () => {
  return (
    <form>
      <label htmlFor="todo">
        Add Todo
        <input type="text" />
      </label>
    </form>
  );
};

export default TodoForm;
